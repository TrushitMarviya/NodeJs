<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book Store</title></head>

    <style>
*{
    margin: 0px;
    padding: 0px;
    box-sizing: border-box;
    font-family: 'Gill Sans', 'Gill Sans MT', Calibri, 'Trebuchet MS', sans-serif;
}
h1{
    font-size: 35px;
}
.container {
padding: 20px;
margin: 5%;
}
.form-container {
width: 400px;
border-radius: 15px;
padding: 35px;
box-shadow: rgba(50, 50, 93, 0.6) 0px 0px 12px -2px, rgba(0, 0, 0, 0.6) 0px 3px 7px -3px;
margin-top: -5%;
}
.form-container form {
display: flex;
flex-direction: column;
gap: 10px;
}

.form-container input,
.form-container button {
padding: 10px;
font-size: 1rem;
margin-bottom: 10px;
border: 1px solid #ddd;
border-radius: 4px;
}

.form-container button {
background-color: #007bff;
color: white;
cursor: pointer;
border: none;
}

.form-container button:hover {
background-color: #0056b3;
}

.card-container {
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        gap: 20px;
        padding: 20px;
    }

    .card {
        border-radius: 12px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        overflow: hidden;
        width: 400px;
        height: auto;
        transition: transform 0.3s, box-shadow 0.3s;
        margin-top: -5%;
        box-shadow: 0 6px 12px rgba(0, 0, 0, 0.6);
    }
    .card-image {
        width: 275px;
        height: 275px;
        margin-top: 25px;
        margin-left: 15%;
        object-fit: contain;
    }

    .card-header h3 {
        width: fit-content;
        color: white;
        text-shadow: 0 2px 4px rgba(0, 0, 0, 0.5);
        background-color: rgba(0, 0, 0, 0.6);
        padding: 5px 10px;
        border-radius: 4px;
        font-size: 1.2rem;
        margin-left: 5%;
    }

    .card-body {
        padding: 16px;
        font-family: Arial, sans-serif;
        color: #333;
        text-align: left;
    }

    .card-body p {
        margin: 8px 0;
    }

    .card-footer {
        padding: 16px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        background-color: #f8f8f8;
    }

    .action-button {
        background-color: #007BFF;
        border: none;
        color: white;
        padding: 8px 12px;
        border-radius: 4px;
        text-transform: uppercase;
        font-size: 14px;
        cursor: pointer;
        transition: background-color 0.3s;
    }

    .action-button a {
        color: white;
        text-decoration: none;
    }

    .action-button:hover {
        background-color: #0056b3;
    }


@media (max-width: 480px) {
    .form-container{
        width: 365px;
        margin-left: -9%;
    }
    .card{
        margin-top: 5%;
    }
}

    </style>
<body>
    <center> <br><br>
    <h1>Book Store</h1>
<div class="container">
    <!-- Form for Adding Data -->
    <div class="form-container">
        <form action="/addData" method="post">
            <input type="text" name="image" placeholder="Enter Image" required>
            <input type="text" placeholder="Book Name" name="name" required>
            <input type="text" placeholder="Author Name" name="author" required>
            <input type="text" placeholder="Book Price" name="price" required>
            <input type="date" name="date" required>
            <button type="submit">Add Book</button>
        </form>
    </div>
</center>

    <div class="card-container">
        <% Books.map((e, i) => { %>
            <div class="card">
                <!-- Card Header with Image -->
                <div class="card-header">
                    <img src="<%= e.image %>" alt="<%= e.name %>" class="card-image"> <br><br>
                    <h3><%= e.name %></h3>
                </div>
                <div class="card-body">
                    <p><strong>Author:</strong> <%= e.author %></p>
                    <p><strong>Price:</strong> <%= e.price %></p>
                    <p><strong>Publish Date:</strong> <%= e.date %></p>
                </div>
                <div class="card-footer">
                    <button class="action-button"><a href="/deleteData?id=<%=e.id%>">Delete</a></button>
                    <button class="action-button"><a href="/editData?id=<%=e.id %>">Edit</a></button>
                </div> <br>
            </div>
        <% }) %>
    </div>
    
</div>
</body>
</html>
